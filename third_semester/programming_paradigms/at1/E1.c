// 1. Usando apenas um comando printf fazer um programa que imprima os valores de variáveis representando a
// população de um país, o peso de uma pessoa e o símbolo do oxigênio

#include <stdio.h>

int main(){
    int pop = 30;
    float peso = 76.5;

    printf("O total de habitantes e de %d milhoes", pop);
    printf("O peso de uma pessoa qualquer e de %f kg", peso);
    prinf("O O2 é fundamental para a sobrevivencia humana");

    return 0;

}